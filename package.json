{
  "name": "sql-runner-proxy",
  "version": "1.0.0",
  "description": "Lightweight read-only SQL proxy for browser widget",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "pg": "^8.11.3"
  }
}
